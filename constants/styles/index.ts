export const styles_Modal = `
   body, main {
     height: 100vh;
     overflow: hidden;
   }
  
 @supports (-webkit-touch-callout: none) {
  html,body, main {
    height: -webkit-fill-available;
  }
}
`;