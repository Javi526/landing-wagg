export const styles_Modal = `
   body, main {
     height: 100vh;
     overflow: hidden;
   }
  
  @media (max-width: 412px) {
      body, main {
       height: 100dvh;
       overflow: hidden;
     }
  }
`;