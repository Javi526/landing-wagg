export const styles_Modal = `
   body, main {
     height: 100vh;
     overflow: hidden;
   }
   @supports(height: 1dvh) {
       body, main {
        height: 1dvh;
        overflow: hidden;
     }
   }
`;